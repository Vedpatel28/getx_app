import 'package:flutter/material.dart';

class Ecom_Cart extends StatelessWidget {
  const Ecom_Cart({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("E Com Cart"),
      ),
    );
  }
}
